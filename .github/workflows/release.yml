name: Release Build

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Build
        run: dotnet build Shoko.Plugin.UploadFile.slnx --configuration Release

      - name: Package
        run: |
          ZIP_NAME="Shoko.Plugin.UploadFile-${{ github.event.release.tag_name }}-any.zip"
          cd source/bin/Release/net10.0
          zip "../../../../${ZIP_NAME}" Shoko.Plugin.UploadFile.dll
          cd ../../../..
          echo "ZIP_NAME=${ZIP_NAME}" >> "$GITHUB_ENV"

      - name: Upload to Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" \
            "${ZIP_NAME}" \
            --repo "${{ github.repository }}"

      - name: Update Manifest
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.event.release.tag_name }}"
          VERSION="${TAG#v}"
          SOURCE_REVISION=$(git rev-parse HEAD)
          RELEASED_AT="${{ github.event.release.published_at }}"
          CHECKSUM=$(sha256sum "${ZIP_NAME}" | awk '{ print $1 }')
          URL="https://github.com/${{ github.repository }}/releases/download/${TAG}/${ZIP_NAME}"

          RELEASE_ENTRY=$(jq -n \
            --arg version "$VERSION" \
            --arg tag "$TAG" \
            --arg source_revision "$SOURCE_REVISION" \
            --arg released_at "$RELEASED_AT" \
            --arg url "$URL" \
            --arg checksum "$CHECKSUM" \
            '{
              version: $version,
              tag: $tag,
              source_revision: $source_revision,
              released_at: $released_at,
              channel: "Stable",
              release_notes: null,
              archives: [
                {
                  runtime: "any",
                  abstraction: "6.0.0",
                  url: $url,
                  checksum: $checksum
                }
              ]
            }')

          jq --argjson release "$RELEASE_ENTRY" '
            .releases |= [ $release ] + (. | map(select(.version != $release.version)))
          ' manifest.json > manifest.json.tmp && mv manifest.json.tmp manifest.json

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add manifest.json
          git commit -m "ci: update manifest for ${TAG}"
          git push origin HEAD:stable
